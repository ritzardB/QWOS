[project]
name = "qwos"
version = "0.1.0"
description = "Quantum Workforce OS Backend API"
readme = "README.md"
requires-python = ">=3.13"
license = { text = "Proprietary" }

keywords = [
    "workforce",
    "human-resources",
    "virtual-assistant",
    "saas",
    "fastapi",
    "postgresql",
]

classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
    "Framework :: FastAPI",
    "Operating System :: OS Independent",
]

authors = [
    { name = "Richard Balabarcon" }
]

dependencies = [
    "fastapi>=0.116.0",
    "uvicorn[standard]>=0.35.0",
    "sqlalchemy>=2.0.41",
    "alembic>=1.16.4",
    "psycopg[binary]>=3.2.9",
    "pydantic>=2.11.7",
    "pydantic-settings>=2.10.1",
    "python-dotenv>=1.1.1"
]

[project.optional-dependencies]
dev = [
    "pytest>=8.4.1",
    "pytest-cov>=6.2.1",
    "httpx>=0.28.1",
    "ruff>=0.12.5",
    "black>=25.1.0",
    "mypy>=1.17.0",
]

# Leave out [project.urls]


[tool.ruff.lint]
select = [
    "E",
    "F",
    "I",
]

docs = [
    "mkdocs",
    "mkdocs-material",
]

[tool.mypy]
python_version = "3.13"
strict = true

[tool.coverage.run]
source = ["src/qwos"]

[tool.black]
line-length = 88
target-version = ["py313"]

[tool.ruff]
line-length = 88
target-version = "py313"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/qwos"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"